
// This file now re-exports everything from AuthProvider.tsx for backward compatibility
import { AuthProvider, useAuth } from './AuthProvider';

export { AuthProvider, useAuth };
export default AuthProvider;
